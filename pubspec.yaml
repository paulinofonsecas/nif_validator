name: nif_validator
description: A package to validate Angolan (NIF).
version: 1.0.0
repository: https://github.com/paulinofonsecas/nif_validator
topics:
  - BI
  - NIF
  - Angola
  - Validator

environment:
  sdk: ^3.3.2

dependencies:
  dio: ^5.4.3+1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
